# Example credentials file (copy to credentials.yaml)
#
# Keep credentials.yaml out of git. It is already ignored via .gitignore.

igdb:
  client_id: "YOUR_IGDB_CLIENT_ID"
  client_secret: "YOUR_IGDB_CLIENT_SECRET"

rawg:
  api_key: "YOUR_RAWG_API_KEY"

